import MapperInterface from "@/components/mapper/mapper-interface"

export default function MapperPage() {
    return (
        <main className="flex h-screen w-full bg-gray-900 text-white overflow-hidden">
            <MapperInterface />
        </main>
    )
}
